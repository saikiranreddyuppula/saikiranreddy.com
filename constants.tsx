export const THREAD_LENGTH_END = 100.0;
export const THREAD_ORIGIN = -38.0;

// Scroll normalization
export const SCROLL_STEP = 60; // Fixed pixels per scroll tick
export const SCROLL_COOLDOWN = 40; // Minimum ms between scroll ticks (caps speed)
export const SCROLL_LERP = 0.08; // Scroll interpolation (lower = smoother)
export const SCROLL_DURATION = 0.9; // Scroll animation duration in seconds
